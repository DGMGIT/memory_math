<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="au.edu.jcu.my.memory_math">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Memory_math">
        <activity
            android:name=".game.twitter.TweetThis"
            android:exported="false" />
        <activity
            android:name=".game.gameDisplay.statistics.Statistics"
            android:exported="false" />
        <activity
            android:name=".game.gameDisplay.setting.Setting"
            android:exported="false" />
        <activity
            android:name=".game.gameDisplay.Play"
            android:exported="false" />
        <activity
            android:name=".game.ModeSelector"
            android:exported="false" />
        <activity
            android:name=".test.TestDB"
            android:exported="false" />
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>